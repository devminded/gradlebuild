apply plugin: 'application'

jar {
    manifest {
        attributes("Implementation-Timestamp": new Date().format("yyyy-MM-dd'T'HH:mm:ss.SSS'Z'", TimeZone.getTimeZone('UTC')))
    }
}

application {
    mainClassName = 'se.devmind.app.AppMain'
}
